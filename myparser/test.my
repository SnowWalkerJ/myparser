x = 3;


function power(base, p) {
    if (p == 1) {
        return base;
    } else {
        return base * power(base, p - 1);
    }
}

function double(x) {
    return x * 2;
}

print double(x);

y = power(3, 3);

print y;
